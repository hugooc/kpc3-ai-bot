# kpc3-ai-bot

An AI-assisted amateur radio packet station running on a **Kantronics KPC-3 Plus** TNC. The station (callsign **W6OAK**, Oakland CA, grid CM87) answers incoming connects over 2m packet and holds a short QSO using Claude Haiku as the control-operator-supervised operator. Ships with a Claude skill that teaches Claude (or any model) how to drive the TNC directly, plus a hand-curated map of the Northern California packet network.

## What this repo actually is

Three independent but related pieces live here:

1. **`w6oak_ai_bot.py`** — a Python bot that listens for incoming connects on the KPC-3+, auto-answers CQs, and chats back using the Anthropic API (Claude Haiku).
2. **`SKILL.md` + `kpc3-packet-station.skill`** — a Claude skill for operating the TNC interactively from the Claude desktop app (Cowork mode). `SKILL.md` is the source; the `.skill` file is the installable bundle.
3. **`NODE_PATHS.md`** — a hand-curated map of which nodes reach which other nodes on 145.050 MHz, used both by humans reading the skill and (via the skill) by the bot when planning multi-hop routes.

All three talk to the TNC over a persistent TCP serial bridge running on a Windows box, so there's no Remote Desktop, no PuTTY scraping, no clipboard hacks.

## Architecture

```
Claude / Bot (TCP) ──── network ──── Windows box on your LAN
                                         │
                                  kpc3_bridge.ps1 (port 8765)
                                         │
                                  COM port (USB serial)
                                         │
                                     KPC-3 Plus TNC
                                         │
                                   2m transceiver
                                         │
                                      Antenna
```

Any client on your LAN can connect to the Windows machine at TCP port 8765 and send/receive raw TNC bytes. PuTTY on the Windows machine can watch everything in real time at `localhost:8765`.

## Folder layout

```
Kantronics KPC3+ Claude Skill/
├── README.md                    ← you are here
├── LICENSE
├── .gitignore
├── sync_public.sh               ← refresh public/ before pushing to GitHub
│
├── SKILL.md                     ← the skill (source of truth, edited here)
├── NODE_PATHS.md                ← reachability map (edited here)
├── NET_CONTROL_2026.md          ← Sunday net-control playbook
├── BOT_DESIGN_2026.md           ← bot v2 architecture spec
├── w6oak_ai_bot.py              ← the bot (edited here)
├── config.example.json
├── KANTRONICS_KPC3_REV-H.md
├── KANTRONICS_KPC3_REV-H.pdf
├── kpc3-packet-station.skill
│
├── public/                      ← sanitized, GitHub-ready copies
│   └── (same files as above, minus secrets — regenerated by sync_public.sh)
│
└── private/                     ← never committed, never shared
    ├── config.json              ← API key + station config
    ├── kpc3_ssh_key             ← SSH key for restarting the Windows bridge
    ├── kpc3_ssh_key.pub
    ├── logs/
    │   ├── w6oak_ai_bot.log
    │   ├── w6oak_packets.log
    │   └── kpc3_bridge.log
    └── archive/
        ├── SKILL.public-354.md.bak
        └── session_debug_log_20260416.md
```

**How it works:** you edit at the root. When you're ready to push a release to GitHub, run `./sync_public.sh` — it copies the public-safe files into `public/` and leaves secrets behind. `private/` is gitignored at the top level, so there's no way to accidentally commit a key or API token.

### What's at the root (active workspace)

| File | Purpose |
|---|---|
| `SKILL.md` | The operating skill. ~1400 lines. Teaches Claude how to drive the KPC-3+: TCP bridge, mode switching, `KPC3Session` helper, KaNode vs K-Net, PBBS, Unproto, diagnostic playbooks. |
| `NODE_PATHS.md` | Hand-curated reachability map for 145.050 MHz in NorCal. Referenced by `SKILL.md` as a companion file. Confirmed edges, degraded paths, a Path Cookbook, and live `NODES`/`ROUTES` captures from OAK, WBAY, and BANNER. |
| `NET_CONTROL_2026.md` | Playbook for running the Sunday 2000 hrs 145.05 Packet Net. Pre-net checklist, echo-test path, regional UNPROTO chains, and the 2021 → 2026 node-rename map. |
| `BOT_DESIGN_2026.md` | Architecture spec for `w6oak_ai_bot.py` v2.0. Connected-only engagement, turn-taking rules, self-protection, lockout flag, logging split, and the negative spec of what the bot must never do. |
| `w6oak_ai_bot.py` | The AI bot. Listens for RING / incoming connects, auto-answers CQs, routes conversations to Claude Haiku via the Anthropic API. Currently v1.2.0. |
| `kpc3_console.py` | Minimal interactive TNC console for the Mac. `python3 kpc3_console.py` gives you a live prompt to drive the TNC over the TCP bridge, with `\r`-only line endings and a background reader for TNC output. See "Operating the TNC manually from the Mac" in `SKILL.md`. |
| `kpc3-packet-station.skill` | Installable skill bundle (zipped). Drag into Claude desktop → Settings → Skills. Rebuild when you ship a change. |
| `config.example.json` | Template config. Copy to `private/config.json` and fill in your own values. |
| `KANTRONICS_KPC3_REV-H.md` / `.pdf` | The KPC-3 Plus User's Guide (Rev H). |
| `sync_public.sh` | Refreshes `public/` from the root. Run before `git push`. |

### What's in `private/`

| Path | Purpose |
|---|---|
| `private/config.json` | Your real config — API key, `tnc_host`, `mycall`. Bot loads this on startup. |
| `private/kpc3_ssh_key` / `.pub` | SSH keypair the skill uses to restart the bridge on the Windows box. |
| `private/logs/` | Rolling logs from the bot and the bridge. Forensic value; safe to prune. |
| `private/archive/` | Old snapshots and one-off debug notes. Keep or delete as you like. |

`private/` never gets committed. The top-level `.gitignore` excludes the whole folder.

### What's in `public/`

A complete, sanitized snapshot ready to `git push` to GitHub. Regenerated by `./sync_public.sh`. Includes everything at the root *except* secrets.

*(The `kpc3_bridge.ps1` PowerShell script that runs the TCP bridge lives on the Windows machine, not in this repo. See "Setting up the TCP bridge" below.)*

## Station info

| | |
|---|---|
| Callsign | **W6OAK** |
| Grid | CM87 (Oakland, CA) |
| PBBS | **OBOX** (`/B`) |
| KaNode | **KOAK** (`/N`) |
| K-Net node | **OAK** (`W6OAK-5`) |
| Digipeater | ON (`/R`) |
| TNC | Kantronics KPC-3+ |
| Bridge | TCP port 8765 on the Windows box |

## Running the AI bot

```bash
pip install anthropic

# One-time config (put it in private/ so it never lands in git)
mkdir -p private
cp config.example.json private/config.json
# edit private/config.json and set:
#   - anthropic_api_key   (from https://console.anthropic.com/settings/keys)
#   - tnc_host            (the LAN IP of your Windows box running the bridge)
#   - tnc_port            (default 8765)
#   - mycall              (your callsign)

python3 w6oak_ai_bot.py
```

The bot looks for `private/config.json` first and falls back to `config.json` at the root if that's missing, so either layout works.

What triggers a Haiku response:

1. **Someone connects to W6OAK directly** (`CONNECT W6OAK` from their TNC). The bot detects RING + `*** CONNECTED to <call>` and opens a chat session.
2. **Someone sends a UI frame addressed to CQ.** The bot auto-connects back and starts a session.

Users sign off with `73`, `BYE`, `B`, `QRT`, `SK`, or `CL`. Idle sessions close after 5 minutes.

The bot keeps a heartbeat log and sets `MCON OFF` during active chats to avoid self-chat loops.

## Installing the Claude skill (interactive use, not the bot)

1. Open the Claude desktop app on macOS.
2. Settings → Skills → Add Skill.
3. Drag `kpc3-packet-station.skill` into the window.
4. Mention the KPC-3, TNC, packet radio, PBBS, KaNode, K-Net, AX.25, etc. in a new Cowork session and the skill will activate.

Example prompts once installed:

- "Send an ID on the packet station"
- "Show me who's been heard recently"
- "Connect to OAK and show me the NODES list"
- "Check my PBBS messages"
- "Plan a path from here to HMKRCH"

The skill loads `NODE_PATHS.md` automatically when multi-hop questions come up.

## Setting up the TCP bridge (Windows side)

The bridge is a PowerShell script (`kpc3_bridge.ps1`) that opens the TNC's COM port at 9600 baud, binds TCP port 8765, and shuttles bytes both ways. Multiple clients can connect at once (bot + PuTTY monitor, for example). Register it as a Windows Scheduled Task named `KPC3Bridge` so it starts at login.

The skill knows how to restart the bridge remotely via SSH using the included `kpc3_ssh_key`.

> **Note:** the bridge script itself is not yet included in this repo. Track [issue #TBD](../../issues) or open a PR if you'd like to contribute one.

Bridge log: `C:\Users\<your-username>\kpc3_bridge.log`

## Compliance and etiquette

- The bot identifies as Claude Haiku in its system prompt and will answer honestly if asked.
- No encryption, no music, no one-way broadcasts. FCC Part 97 compliant.
- Keep replies under 120 chars. 1200 baud isn't infinite bandwidth.
- The beacon clearly announces "AI-assisted station" to give other ops a heads-up before they connect.

## Contributing

Issues and PRs welcome, especially:

- Better CQ detection (current regex only matches destination-field CQ, not payload CQ)
- Additional node-network awareness in the system prompt
- Support for TNC firmware variants beyond the KPC-3+
- A clean copy of `kpc3_bridge.ps1` for the Windows side

## License

MIT — see [LICENSE](LICENSE).

The idea, design, and direction of this project are by Hugo Campos (W6OAK).
The code was authored by Claude (Anthropic's AI assistant) at Hugo's request
and under his supervision.

## 73

de W6OAK
